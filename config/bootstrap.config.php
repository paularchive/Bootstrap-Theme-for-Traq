<?php
/* This is going to be the config file for the bootstrap theme, but I don't know how I'm going to use it */


# Should we use an inverted navbar?
# Nah I'm not going to do it this way:
/* $config = parse_ini_file( APPPATH . '/views/' . settings('theme') . '/config/config.ini' );

if ($config['navbar'] == "inverse") {
	$navbar_inverted = true;
} else {
	$navbar_inverted = false;
} */

?>